--- a/primitive.cabal
+++ b/primitive.cabal
@@ -64,7 +64,7 @@
   Install-Includes: primitive-memops.h
   includes: primitive-memops.h
   c-sources: cbits/primitive-memops.c
-  if !os(solaris)
+  if !os(solaris) && !os(irix)
       cc-options: -ftree-vectorize
   if arch(i386) || arch(x86_64)
       cc-options: -msse2
